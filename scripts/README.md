# Scripts

Reserved for standalone scripts (e.g. running the change point model outside
a notebook, or exporting results for the Task 3 dashboard). Empty for now -
Task 1 logic lives in `src/` and `notebooks/`.
